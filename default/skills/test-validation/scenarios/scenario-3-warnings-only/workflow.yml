name: scenario-3-clean-workflow
on:
  push:
    branches: [main]
permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "all checks pass - pinned SHA, has permissions, has on trigger"
