# Architecture Principles

- Prefer explicit boundaries between modules and services.
- Keep coupling visible — dependencies should be declared, not hidden.
- Avoid speculative architecture — build for current needs, design for extensibility.
- Scale complexity only when justified by evidence.
- Favor composition over inheritance.
- API contracts are the integration boundary — keep them stable and versioned.
