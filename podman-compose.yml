# =======================================================
# Local runner for .gitlab-ci.yml validation jobs
# =======================================================
#
# Usage (podman-compose or docker-compose):
#
#   1. Copy .env.example → .env and fill in the values.
#   2. Run ALL validators + station orchestrator:
#        podman-compose up --abort-on-container-exit
#   3. Run only the Python validators (no Copilot CLI needed):
#        podman-compose up validate-pr-auto validate-yaml-workflows validate-test-gaps
#   4. Run only the station orchestrator:
#        podman-compose up stations-run-all
#
# Reports are written to ./outputs/reports/ on your host.
# Station outputs are written to ./outputs/station_out/ on your host.
# =======================================================

version: "3.8"

x-common-env: &common-env
  CI_PROJECT_DIR: /workspace
  VALIDATORS_DIR: .apm/skills/ai-backbone-pr-checks/tools/scripts
  REPORTS_DIR: outputs/reports
  STATION_OUT: outputs/station_out
  STATION_DIR: ci-gates/stations
  STATION_TIMEOUT: "900"
  CI_MERGE_REQUEST_TARGET_BRANCH_NAME: ${CI_MERGE_REQUEST_TARGET_BRANCH_NAME:-main}
  CI_COMMIT_SHA: ${CI_COMMIT_SHA:-HEAD}
  CI_MERGE_REQUEST_IID: ${CI_MERGE_REQUEST_IID:-0}
  COPILOT_MAX_DIFF_LINES: "2000"
  COPILOT_CLI_VERSION: "1.0.4"
  ENABLE_COPILOT_CLI: ${ENABLE_COPILOT_CLI:-false}
  GITHUB_TOKEN: ${GITHUB_TOKEN:-}

x-repo-volume: &repo-volume
  - .:/workspace:z

services:
  # ---------------------------------------------------
  # Python-only validators  (no Copilot CLI required)
  # ---------------------------------------------------

  validate-pr-auto:
    image: python:3.11
    working_dir: /workspace
    environment:
      <<: *common-env
    volumes: *repo-volume
    entrypoint: ["/bin/bash", "-ec"]
    command:
      - |
        apt-get update -qq && apt-get install -y -qq git
        python3 -m pip install --upgrade pip PyYAML -q
        git config --global --add safe.directory /workspace

        mkdir -p $REPORTS_DIR
        python3 $VALIDATORS_DIR/pr_auto_validator.py \
          --base-ref $CI_MERGE_REQUEST_TARGET_BRANCH_NAME \
          --head-ref $CI_COMMIT_SHA \
          --out $REPORTS_DIR/pr-auto-validator.json

        echo "=== PR Auto Validator result ==="
        cat $REPORTS_DIR/pr-auto-validator.json

  validate-yaml-workflows:
    image: python:3.11
    working_dir: /workspace
    environment:
      <<: *common-env
    volumes: *repo-volume
    entrypoint: ["/bin/bash", "-ec"]
    command:
      - |
        python3 -m pip install --upgrade pip PyYAML -q

        mkdir -p $REPORTS_DIR
        python3 $VALIDATORS_DIR/yaml_workflow_linter.py \
          --root . \
          --out $REPORTS_DIR/yaml-workflow-linter.json

        echo "=== YAML Workflow Linter result ==="
        cat $REPORTS_DIR/yaml-workflow-linter.json

  validate-test-gaps:
    image: python:3.11
    working_dir: /workspace
    environment:
      <<: *common-env
    volumes: *repo-volume
    entrypoint: ["/bin/bash", "-ec"]
    command:
      - |
        apt-get update -qq && apt-get install -y -qq git
        python3 -m pip install --upgrade pip PyYAML -q
        git config --global --add safe.directory /workspace

        mkdir -p $REPORTS_DIR
        python3 $VALIDATORS_DIR/test_gap_detector.py \
          --base-ref $CI_MERGE_REQUEST_TARGET_BRANCH_NAME \
          --head-ref $CI_COMMIT_SHA \
          --out $REPORTS_DIR/test-gap-detector.json

        echo "=== Test Gap Detector result ==="
        cat $REPORTS_DIR/test-gap-detector.json

  # ---------------------------------------------------
  # AI Station Orchestrator (requires GitHub Copilot CLI + token)
  # ---------------------------------------------------
  # Dynamically discovers all station files and runs them
  # sequentially. Mirrors the stations:run-all CI job.
  # ---------------------------------------------------

  stations-run-all:
    image: node:20-bookworm-slim
    working_dir: /workspace
    depends_on:
      - validate-pr-auto
      - validate-yaml-workflows
    environment:
      <<: *common-env
    volumes: *repo-volume
    entrypoint: ["/bin/bash", "-ec"]
    command:
      - |
        apt-get update -qq && apt-get install -y -qq git jq curl ca-certificates python3 findutils
        npm install -g "@github/copilot@$${COPILOT_CLI_VERSION}"
        git config --global --add safe.directory /workspace

        if git rev-parse --is-shallow-repository 2>/dev/null | grep -q true; then
          git fetch --unshallow
        fi

        mkdir -p $$STATION_OUT

        bash /workspace/ci-gates/scripts/run_stations.sh
